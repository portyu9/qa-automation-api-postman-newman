{
  "name": "qa-automation-api-postman-newman",
  "version": "1.0.0",
  "description": "API automation suite using Postman collections and Newman",
  "scripts": {
    "test": "mkdir -p reports && newman run collections/jsonplaceholder.postman_collection.json -e postman_environment.json -r cli,html --reporter-html-export reports/html-report.html"
  },
   "devDependencies": {
    "newman": "^6.3.0",
    "newman-reporter-html": "^1.0.7",
    "ajv": "^8.12.0"
  }
}
