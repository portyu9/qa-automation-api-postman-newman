{
  "name": "qa-automation-api-postman-newman",
  "version": "1.0.0",
  "private": true,
  "description": "Postman collection and Newman execution framework with schema, data, deterministic target, and CI reporting controls.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node scripts/run-newman.js",
    "test:smoke": "NEWMAN_FOLDER=Read node scripts/run-newman.js",
    "validate": "node scripts/validate-assets.js && node scripts/runtime.selftest.js && node scripts/local-api.selftest.js && node scripts/execution-ledger.selftest.js"
  },
  "license": "MIT",
  "devDependencies": {
    "newman": "6.2.2"
  }
}
